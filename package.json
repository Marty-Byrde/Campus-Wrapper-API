{
  "name": "ts-template",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "dev": "ts-node tests",
    "build": "tsc --build tsconfig.json",
    "prepublish": "tsc --build tsconfig.json"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
